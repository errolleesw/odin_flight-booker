<%= form_with(model: passenger_booking) do |form| %>
  <% if passenger_booking.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(passenger_booking.errors.count, "error") %> prohibited this passenger_booking from being saved:</h2>

      <ul>
        <% passenger_booking.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :booking_id, style: "display: block" %>
    <%= form.text_field :booking_id %>
  </div>

  <div>
    <%= form.label :passenger_id, style: "display: block" %>
    <%= form.text_field :passenger_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
